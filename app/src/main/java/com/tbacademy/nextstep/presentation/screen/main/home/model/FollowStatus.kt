package com.tbacademy.nextstep.presentation.screen.main.home.model

import com.tbacademy.nextstep.R

enum class FollowStatus(val textRes: Int) {
    TO_FOLLOW(textRes = R.string.follow),
    FOLLOWED(textRes = R.string.followed)
}